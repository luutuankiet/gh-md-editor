node_modules/
dist/
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
gsd-lite/
.env
.env.*
!.env.example
.svelte-kit/
.vite/
# Everything under .claude/ is private EXCEPT the two index skills, which are how
# a session finds docs/ at all. The trailing /* matters: git will not descend into
# an excluded directory, so a plain `.claude/` cannot be re-included below.
.claude/*
!.claude/skills/
Bn\\
